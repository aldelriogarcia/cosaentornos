/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package ruleta;

import java.util.Scanner;
import java.util.Random;

/**
 *
 * @author dawmi
 */
public class Ruleta {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
    Scanner teclado= new Scanner(System.in);    
    double num1=99999;
    double dinerojugd = 1;
    double dinerojugd1 = 1;
    double dinerojugd2=1;
    double dinerorul;
    int apuesta;
    int numapuesta;
    
        System.out.println("Introduce el dinero que tengas");
             dinerojugd=teclado.nextInt();
             dinerojugd2=dinerojugd;
             dinerorul = 100*dinerojugd;
                
        while ((dinerojugd1<0)){
                System.out.println("Debe ser una cantidad mayor que 0");
                dinerojugd=teclado.nextInt();
                dinerorul = 100*dinerojugd;
            }
              
        while ((dinerojugd!=0)||(dinerojugd>(dinerojugd2*50))){ 
           
        System.out.println("Introduce un número entre 16 y"
                    + " 36 múltiplo de 4");
        num1=teclado.nextInt();   
        
        while ((num1<16) || (num1>36) || (num1%4!=0)){
            System.out.println("El número introducido no es válido, "
                    + "introduce otro");
             num1=teclado.nextInt();
            }

            System.out.println("Elige la cantidad a apostar ");                       
                 apuesta=teclado.nextInt();
            System.out.println("Y el número por el que lo apuestas, entre 0 y "
            +(int)num1);
                 numapuesta=teclado.nextInt();

        while ((apuesta<0)||(apuesta>dinerojugd)){
            System.out.println("Los apuesta introducida no es válida,"
               + " prueba otra vez");
       
            System.out.println("Elige la cantidad a apostar ");                       
                 apuesta=teclado.nextInt();
            }

        while ((numapuesta<0)||(numapuesta>num1)){
                 System.out.println("El número elegido  no es válido,"
               + " prueba otra vez");

                System.out.println("Elige el número por el que lo apuestas");
                 numapuesta=teclado.nextInt();
            }
            
        Random rd = new Random();
        int numeroRuleta = rd.nextInt(num1+1);
        
        if (numeroRuleta==numapuesta){
             dinerojugd1=apuesta*16;
            dinerojugd = dinerojugd1 + dinerojugd;
            System.out.println("has aceptado");
        }
        else if (dinerojugd%2 == numeroRuleta%2) {
                dinerojugd1 = apuesta*1.8;
                dinerojugd = dinerojugd+dinerojugd1;
                dinerorul = dinerorul - dinerojugd1;
        } 
        else {
        dinerorul = dinerorul+apuesta;
        dinerojugd = dinerojugd - apuesta;
        }  

        dinerorul = 100*dinerojugd;
        System.out.println("El número seleccionado por"
                    + " la ruleta fue "+(int)aleatoria);
        System.out.println("La apuesta fueron "+apuesta+" al "+numapuesta);
        System.out.println("La ganancia ha sido "+(int)dinerojugd1);
        System.out.println("El dinero del jugador es: "+(int)dinerojugd);
        System.out.println("El dinero inicial del jugador era: "
                 +(int)dinerojugd2);           
        System.out.println("La ruleta tiene "+(int)dinerorul);
        System.out.println("");

    }
    }  
}
